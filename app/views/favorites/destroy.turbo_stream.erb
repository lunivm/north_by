<%= turbo_stream.remove(dom_id(@favorite)) %>
<%= turbo_stream.replace(dom_id(@favorite.concert), @favorite.concert) %>
<% if current_user.favorites.empty? %>
  <%= turbo_stream.append("favorite-concerts") do %>
    <%= render "favorites/no_favorites"%>
  <% end %>
<% end %>
