%p#notice= notice
%h1 Gigs
%table
  %thead
    %tr
      %th Band
      %th Concert
      %th Order
      %th Duration minutes
      %th{:colspan => "3"}
  %tbody
    - @gigs.each do |gig|
      %tr
        %td= gig.band_id
        %td= gig.concert_id
        %td= gig.order
        %td= gig.duration_minutes
        %td= link_to "Show", gig
        %td= link_to "Edit", edit_gig_path(gig)
        %td= link_to "Destroy", gig, method: :delete, data: {confirm: "Are you sure?"}
%br/
= link_to "New Gig", new_gig_path
