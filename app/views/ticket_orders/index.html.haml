%p#notice= notice
%h1 Ticket Orders
%table
  %thead
    %tr
      %th Concert
      %th{:colspan => "3"}
  %tbody
    - @ticket_orders.each do |ticket_order|
      %tr
        %td= ticket_order.concert_id
        %td= link_to "Show", ticket_order
        %td= link_to "Edit", edit_ticket_order_path(ticket_order)
        %td= link_to "Destroy", ticket_order, method: :delete, data: {confirm: "Are you sure?"}
%br/
= link_to "New Ticket Order", new_ticket_order_path
